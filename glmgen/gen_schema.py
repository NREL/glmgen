
def main(gld_install_path):

    # get gld version

    # get list of candidate module names
    candidate_modules = [] # should be set, not list
    # file names in lib
        
    # module XXXX calls in samples .glms
    

    # populate schema by scraping modhelp
    for candidate in candidate_modules:
        "gridlabd --modhelp {}".format(candidate)
        
if __name__ == "__main__":
    # get gld_install_path and out_dir
    
    main(gld_install_path)
        
    
